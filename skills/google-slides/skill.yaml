name: google-slides
version: 1.0.0
runtime:
  env: []
  executables:
    - node
    - workspace-cli
  pythonModules: []
  nodeModules: []
healthcheck:
  command: node --version >/dev/null && workspace-cli --help >/dev/null