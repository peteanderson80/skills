name: sora
version: 1.0.0
runtime:
  env:
    - OPENAI_API_KEY
  executables:
    - python3
  pythonModules:
    - openai
  nodeModules: []
healthcheck:
  command: python3 -c "import openai" && test -n "$OPENAI_API_KEY"